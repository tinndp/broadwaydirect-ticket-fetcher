[pytest]
# every crawler keeps its tests in <package>/tests; imports are absolute from python/
pythonpath = .
testpaths = shared broadwaydirect stubhub paciolanevenue axs
addopts = --import-mode=importlib
