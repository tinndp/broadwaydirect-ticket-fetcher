{
  "name": "broadwaydirect-show-discovery-crawler",
  "version": "1.0.0",
  "private": true,
  "description": "Node/TypeScript port of the Python discovery crawler - discovers every Broadway Direct (Tixtrack) show and every scheduled performance.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cli": "node dist/cli.js",
    "postinstall": "patchright install chromium"
  },
  "dependencies": {
    "patchright": "^1.63.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
